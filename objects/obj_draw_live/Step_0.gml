if (instance_exists (obj_c1))
{
      numGauge = round((global.vidap1 * numLength) / global.MAXvidap1);
}

else

if (instance_exists (obj_cc1))
{
      numGauge = round((global.vidap1 * numLength) / global.MAXvidap1);
}

else

if (instance_exists (obj_d1))
{
      numGauge = round((global.vidap1 * numLength) / global.MAXvidap1);
}

else

if (instance_exists (obj_o1))
{
numGauge = round((global.vidap1 * numLength) / global.MAXvidap1);
}

else

if (instance_exists (obj_g1))
{
    numGauge = round((global.vidap1 * numLength) / global.MAXvidap1);
}

else

if (instance_exists (obj_ta1))
{
   numGauge = round((global.vidap1 * numLength) / global.MAXvidap1);
}


else

if (instance_exists (obj_tb1))
{
    numGauge = round((global.vidap1 * numLength) / global.MAXvidap1);
}


else

if (instance_exists (obj_tc1))
{
    numGauge = round((global.vidap1 * numLength) / global.MAXvidap1);
}

else

if (instance_exists (obj_co1))
{
    numGauge = round((global.vidap1 * numLength) / global.MAXvidap1);
}

else

if (instance_exists (obj_l1))
{
    numGauge = round((global.vidap1 * numLength) / global.MAXvidap1);
}

else

if (instance_exists (obj_e1))
{
    numGauge = round((global.vidap1 * numLength) / global.MAXvidap1);
}

else

if (instance_exists (obj_v1))
{
    numGauge = round((global.vidap1 * numLength) / global.MAXvidap1);
}

